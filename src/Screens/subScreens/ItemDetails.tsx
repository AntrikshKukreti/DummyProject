import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const ItemDetails = (
  props: Props,
  // , {details: {}}
) => {
  // const {email} = route.params;
  return (
    <View>
      <Text style={{color: '#000'}}>ItemDetails</Text>
    </View>
  );
};

export default ItemDetails;

const styles = StyleSheet.create({});
